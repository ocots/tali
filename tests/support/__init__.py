"""Outillage de test uniquement : rastérisation et déformations synthétiques.

Rien ici n'est importé par `src/tali/**`. `pypdfium2` (décision 0002) n'est une
dépendance que pour rastériser dans les tests — jamais à l'exécution.
"""
